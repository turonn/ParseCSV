<div style="text-align: center;">
<h2>Upload File</h2>
  <h4>These are the requirements to upload...</h4>
  <p>- Expecting a .txt file separated by spaces</p>
  <p>- Each line must start with the key word "Driver" or "Trip"</p>
  <p>- Driver lines must have exactly 2 elements</p>
  <p>- Trip lines must have exactly 5 elements</p>
  <p>- Times must be in xx:xx or x:xx format</p>
  <p>- Drivers can only have one name ex: "Dan" or "Seri"</p>
  
  <br/>
  <br/>
  
  <h4>Upload Driving Records</h4>

  <%= form_tag import_driving_records_path, multipart: true do %>

    <label for="file">File to Upload</label> <%= file_field_tag :file %>
    <%= submit_tag "Upload Records", class:"button-submit", accept: 'text/txt' %>
  
  <% end %>

  <br/>

</div>
<%= button_to "back to driving records", driving_records_path, method: :get, class:"button" %>

<br/>

<%= button_to "clear database", clear_database_path, method: :delete, class:"button" %>